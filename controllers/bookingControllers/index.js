const { getAllBookings, getBooking, addBooking, updateBooking, deleteBooking, } = require("./bookingControllers")

module.exports = {
    getAllBookings, getBooking, addBooking, updateBooking, deleteBooking,
}