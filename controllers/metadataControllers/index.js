const {
    getMetadata,
    addMetadata,
    updateMetadata,
    deleteMetadata,
} = require("./metadataControllers")

module.exports = {
    getMetadata,
    addMetadata,
    updateMetadata,
    deleteMetadata,
}