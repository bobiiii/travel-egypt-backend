const {
    getMetadata,
    getAllMetadata,
    addMetadata,
    updateMetadata,
    deleteMetadata,
} = require("./metadataControllers")

module.exports = {
    getMetadata,
    getAllMetadata,
    addMetadata,
    updateMetadata,
    deleteMetadata,
}