const {
  getAllCategories, getCategory, addCategory, updateCategory, deleteCategory,
} = require('./categoryControllers');

module.exports = {
  getAllCategories, getCategory, addCategory, updateCategory, deleteCategory,
};
